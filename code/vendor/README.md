# Vendor And Helper Scripts

`convert_playermodel (Manual).py` is still kept at the add-on root for compatibility with the existing PMX conversion flow.

Do not move it into this folder until the runtime lookup and a Blender background smoke test are updated together.
